server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: if the requested path isn't a real file, serve index.html
    # so React Router can handle client-side routes like /goals on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
